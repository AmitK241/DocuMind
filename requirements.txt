# ── Core LangChain stack ───────────────────────────────────────────────────────
# Text splitters moved to dedicated package in LangChain 0.2+
langchain-text-splitters>=0.3.0
langchain>=0.3.26
langchain-community>=0.3.25
langchain-core>=0.3.58
langchain-groq>=0.3.2
langchain-huggingface>=0.1.2
langsmith>=0.2.0

# ── Vector store ───────────────────────────────────────────────────────────────
# chromadb 0.5.x requires SQLite >= 3.35 which Streamlit Cloud lacks.
# pysqlite3-binary ships a modern SQLite and is monkey-patched in app.py.
chromadb==0.5.23
pysqlite3-binary

# ── Embeddings ─────────────────────────────────────────────────────────────────
# sentence-transformers 3.3.1 is incompatible with langchain-huggingface >= 0.1;
# 3.3.1 also requires numpy<2 which conflicts with newer ST builds.
sentence-transformers>=3.4.1

# ── PDF parsing ────────────────────────────────────────────────────────────────
pypdf>=5.1.0

# ── Web framework ──────────────────────────────────────────────────────────────
streamlit>=1.41.1

# ── Utilities ──────────────────────────────────────────────────────────────────
python-dotenv>=1.0.1
tavily-python>=0.5.0
numpy>=1.26.0